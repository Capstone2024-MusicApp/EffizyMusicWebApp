@using EffizyMusicSystem.Models

@if (InstructorProfile != null)
{
    <h4>Edit Instructor Profile</h4>
    <form @onsubmit="@HandleSubmit">
        <div class="form-group">
            <label for="firstName">First Name</label>
            <input id="firstName" class="form-control" @bind="@InstructorProfile.FirstName" />
        </div>
        <div class="form-group">
            <label for="lastName">Last Name</label>
            <input id="lastName" class="form-control" @bind="@InstructorProfile.LastName" />
        </div>
        <!-- Add more fields as needed -->
        <button type="submit" class="btn btn-primary">Save</button>
    </form>
}
else
{
    <p>Loading...</p>
}

@code {
    [Parameter]
    public Instructor InstructorProfile { get; set; }

    [Parameter]
    public EventCallback<Instructor> OnSubmit { get; set; }

    private async Task HandleSubmit()
    {
        await OnSubmit.InvokeAsync(InstructorProfile);
    }
}